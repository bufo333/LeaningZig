# Exercise 28.1: a typed design catalogue

Edit `src/designs.zig` and `src/designs.zon`. The `.zon` file sits next to the
source so `@import("designs.zon")` works.

Run the tests:

    zig build test --summary all
