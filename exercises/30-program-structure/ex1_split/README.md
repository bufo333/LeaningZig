# Exercise 30.1: one program, several files

`src/main.zig` imports `domain/types.zig`, `domain/pilot.zig` and `roster.zig`.
Finish `src/roster.zig`.

    zig build run
    zig build test --summary all
