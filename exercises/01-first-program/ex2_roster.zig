//! Chapter 01, exercise 2: three lines, one placeholder each.
//! Run with:  zig run ex2_roster.zig
//! Expected output (on stderr):
//!   Company: Grey Death Legion
//!   Commander: Carlyle
//!   Units: 4
//! This starter does not compile until you use every constant.
const std = @import("std");

pub fn main() void {
    const company = "Grey Death Legion";
    const commander = "Carlyle";
    const units: u32 = 4;
    // TODO: print the three lines shown in the header, using {s} for text
    // and {d} for the number. Every value above must be used.
}
